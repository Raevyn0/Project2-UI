version:
  phases:
    install:
      runtime-versions:
        nodejs: latest
     pre-build:
        commands:
          - npm install
     build:
         commands:
          - npm run build

    artifacts:
      files:
      - '**/*'
    base-directory: 'build'
